node_modules
.next
*.db
*.db-journal
.env
.env.local
.env*.local
# Runtime user uploads (team logos etc.) are not part of the build
/public/uploads/
.cognito_tmp/

# scratch folder for files Claude could not delete on the mount
_to_delete/
